# Capture baseline evidence

`pnpm capture:baseline` converts the latest completed NexusReach and SignalDraft pilot runs into a private, machine-readable quality baseline. It does not start either product, replay a flow, contact a provider, or copy media into Git. It inspects the already-retained private pilot artifacts and writes `tmp/capture-baseline/baseline-report.json` with mode `0600`.

## Safety boundary

- The registered pilot and workflow identities come from `capture-pilots/baseline.json`.
- Latest pointers contribute only a runtime-validated run ID. Their stored absolute paths are ignored; report and checkpoint paths are derived below the registered pilot root.
- Every inspected media, caption, quality-report, or contact-sheet artifact must be a regular non-symlink file below that run's `private-artifacts` directory. Contact sheets must carry a JPEG signature, and stored quality identity must match the presentation result.
- FFprobe is invoked with an argument array, timeout, and bounded output.
- The emitted report contains artifact IDs, hashes, byte counts, media probes, interaction counts, safe quality status/check codes, coverage counts, and repository-evidence hashes. It omits local paths, object keys, local URLs, verification payloads, caption text, credentials, pixels, and product content.
- A failed threshold produces a stable finding code and a nonzero CLI exit. The command does not alter or delete pilot artifacts.

## Version 1 acceptance thresholds

The committed configuration currently requires:

- 5–180 seconds per clip;
- at least 1280×720 for normalized landscape clips;
- exactly 1080×1920 for vertical renders;
- H.264 video and AAC audio on vertical renders;
- 24–60 frames per second;
- a visible pointer with at least 250 ms movement presentation;
- character typing delay from 15–80 ms;
- an editable WebVTT track containing at least one cue;
- one versioned private quality report and one private JPEG contact sheet per workflow;
- no failed automated quality gate; warning-class findings remain visible for human review;
- all sampled vertical frames to pass the existing informative-frame QA;
- completed, verified attempts for every registered workflow; and
- complete declared-goal and approved-flow coverage with no uncovered or blocked entries.
- a current `capture-coverage-v2` snapshot backed by `capture-coverage-inventory-v1` for every registered pilot.

These thresholds establish a regression baseline. Automated effective-text and caption-layout checks use declared typography and deterministic composition geometry rather than OCR or perceptual review. They do not prove human comprehension, actual mobile-device legibility, aesthetic quality, or exhaustive product coverage.

## Retained local evidence — 2026-07-15

The Phase 4 report generated by the command passed with:

- two independent real-product pilots;
- seven of seven registered workflows verified;
- seven normalized landscape clips;
- seven 1080×1920 vertical renders;
- seven editable caption tracks;
- seven private quality reports and seven private JPEG contact sheets;
- four `ready` outputs, three warning-class outputs, and zero failed quality gates;
- two current versioned coverage snapshots and zero stale or unknown snapshots;
- zero threshold failures; and
- three review warnings.

The NexusReach baseline references run `2026-07-15T06-38-28-916Z-b6bfb8ca-e2e5-4efb-92c3-5067495fe2f0`. The SignalDraft baseline references run `2026-07-15T06-45-37-346Z-04b089ed-4d32-4f75-af1b-1348374b7f77`. NexusReach onboarding warns on an observed loading state; both SignalDraft flows warn on captions that remain longer than the preferred range. The generated JSON and referenced private media remain ignored by Git.

## Current implementation inventory

| Boundary | Authoritative implementation | Baseline evidence |
| --- | --- | --- |
| Capture execution | `captureRunWorker.ts`, `playwrightCaptureExecutor.ts`, `capturePilot.ts` | Real dry run, reset, recording, verification, normalization, artifacts, and checkpoints across seven flows |
| Presentation rendering | `capturePresentationRenderer.ts`, `media.ts` | Seven 1080×1920 H.264/AAC outputs, seven WebVTT tracks, informative-frame validation |
| Automated quality | `captureVideoQuality.ts`, `captureQualityThresholds.json` | Seven private reports/contact sheets; four ready, three warnings, zero failures across both real products |
| Coverage | `captureCoverageInventory.ts`, `captureCoverage.ts`, `captureCoverageService.ts`, `postRunCoverage.ts` | Both pilots have current v2 snapshots; declared goal/flow plus bounded persona/route/state/sequence/outcome/failure inventories are traceable, while untrusted feature-flag denominators remain explicitly unknown |
| Retry and recovery | `captureExecutionRetry.ts`, capture run coordinator/worker state machines | Bounded one-flow retry, durable checkpoint, failure preservation, and successful real-product retries |
| Deletion | `captureDeletion.ts`, PostgreSQL capture repository and migration | Workspace-scoped row purge, artifact collection, and opaque credential cleanup/reconciliation contract; deployed object lifecycle remains external |
| Browser isolation | `isolatedCaptureRuntime.ts`, capture worker configuration check, network policy/probe | Manifest-bound remote attestation and production fail-closed configuration; an exercised disposable local container remains a later phase |
| Hosted review UI | `apps/web` capture workspace, proxy, typed client, and Playwright E2E | Connect, discover, edit, approve, capture, preview, coverage, retry, assembly, and unavailable-runtime states are automated |
| Baseline reporting | `captureBaselineReport.ts`, `capture-pilots/baseline.json` | Private non-symlink artifact enforcement, FFprobe/JPEG/quality identity inspection, explicit thresholds, path-free JSON output, and symlink overwrite rejection |
