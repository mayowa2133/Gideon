# Creator-video gap and evidence matrix

Updated: 2026-07-21

This is the live implementation ledger for the creator-video gap-closing goal. A row moves to **Complete — local** only after the implementation, focused tests, benchmark evidence, and documentation agree. External confirmations are never used to defer locally testable engineering.

| Requirement | Current implementation | Missing behavior | Implemented change | Tests | Completion evidence | Remaining external confirmation |
| --- | --- | --- | --- | --- | --- | --- |
| Encoded single-scene regeneration | **Complete — local.** Versioned, content-addressed scene segments are rendered and spliced atomically; scoped requests invalidate the requested scene plus direct transition neighbours and preserve manual overrides. | None locally. | Added render scope, deterministic cache lineage, per-scene encoding, validation receipts, private segment/manifest artifacts, real UI action, and reused/regenerated scene state. | `sceneRenderCache.test.ts`, `jobExecutor.test.ts`, `store.test.ts`, media tests, full serial suite. | Benchmark request for `scene-003` regenerated `scene-002`–`scene-004`, byte-reused 15 other encoded segments, and passed all seven boundary/timeline/audio/caption/codec validations. | None. |
| Final-render frozen-frame detection | **Complete — local.** Final validation performs scene-aware black, blank, repeated-frame, unexpected-freeze, and stale-loop analysis. | None locally. | Added dense temporal sampling, motion-expectation policy, structured receipt, and blocking `temporal_signal` gate with intentional-static exceptions. | `creatorVideoTemporalQuality.test.ts`, `creatorVideoQuality.test.ts`, full serial suite. | Benchmark sampled 114 frames; repeated-frame ratio `0.4706`; longest unexpected freeze `1200 ms`; no affected, stale-loop, black, or blank scenes; result `pass`. | Production media may be used to recalibrate thresholds; this does not block the implemented gate. |
| Production product-asset materialization | **Complete — local.** Normal render jobs materialize and persist approved assets before rendering and reuse matching private artifacts. | None locally. | Added deterministic factory/version/hash lineage, stale-input invalidation, approval/masking enforcement, artifact persistence, blueprint update, and factual/conceptual separation. | `productAssetFactory.test.ts`, `jobExecutor.test.ts`, `storage.test.ts`, full serial suite. | Normal-job integration passes; benchmark manifest contains 10 hashed assets with bounded path-free metadata and all supported kinds. | None. |
| Distinct product treatments | **Complete — local.** The compositor has a deterministic branch for every supported kind, including temporal interaction media. | None locally. | Added clean screenshot, interaction clip, browser, phone, terminal, before/after, feature, evidence comparison, hero, and labelled conceptual treatments. | `productTreatments.test.ts`, media tests, benchmark. | All 10 kinds appear in the benchmark manifest and inspected contact sheet with distinct visual structures. | Brand visual approval only. |
| Asset review previews | **Complete — local.** The review panel renders screenshot thumbnails and playable interaction clips with lineage, treatment, factual/conceptual, masking, approval, replacement, accessibility, and fallback states. | None locally. | Added authorized/local preview rendering and compatible approved replacement controls without creating a general asset manager. | Renderer typecheck, store tests, desktop production build. | `pnpm typecheck` and `pnpm build:desktop` pass; visual-only blueprint persistence retains script and narration lineage. | Hosted signed-access behavior still requires the production storage canary already listed in the requirements matrix. |
| Serif/italic typography | **Complete — local.** Kinetic and editorial families resolve separately, with deterministic fallback and render receipts. | None locally. | Added a genuine locally installed serif italic resolver, explicit italic styling/wrapping/line height, and requested/resolved/fallback metadata. | `mediaTypography.test.ts`, media tests, benchmark. | Benchmark resolved kinetic to `Arial Bold.ttf` and editorial to `Georgia Italic.ttf` with `italic: true`; both families appear in the contact sheet. | Legal approval is required only before bundling a commercial production font; current local validation uses system fonts. |
| Pronunciation execution | **Complete — local.** The normalized dictionary changes only provider speech input and participates in voiceover/avatar reuse lineage. | None locally. | Added bounded validation, Unicode-safe longest-term substitution fallback, deterministic hashing, provider request support, provenance, and dependent-artifact invalidation. | `pronunciation.test.ts`, `providers/openai.test.ts`, `jobExecutor.test.ts`, full serial suite. | Benchmark records dictionary hash `61fabfa0…e225fc`; tests prove captions/approved scripts stay unchanged while provider input and cache identity change. | Provider-specific pronunciation canary and human review. |
| Rectangle collision avoidance | **Complete — local.** Caption/heading placement uses actual normalized presenter, product-readable, disclosure, CTA, and safe-area rectangles. | None locally. | Added candidate placement, intersection checks, deterministic scale/reflow, stored receipts, and fail-closed impossible-layout gating. | `creatorVideoLayout.test.ts`, `creatorVideoQuality.test.ts`, benchmark. | Benchmark stored collision-free placements for every rendered scene and reported no impossible scene IDs; tests cover every required layout plus an impossible fixture. | Provider landmarks/segmentation may refine presenter rectangles later. |
| Documentation accuracy | **Complete — local.** Architecture, lifecycle, thresholds, command contract, security, UX, API projection, and requirements status reflect the implemented behavior. | None locally. | Reconciled the production guide, requirements matrix, technical spec, testing strategy, security rules, UX flow, API contract, `AGENTS.md`, and this ledger. | Documentation/search audit, lint, complete diff review. | No local gap remains labelled as future work; all 13 separately actionable external-confirmation categories remain in the requirements matrix. | The 13 categories remain explicitly external. |
| First-class benchmark command | **Complete — local.** `pnpm creator-video:benchmark` invokes the existing CLI and defaults to ignored `tmp/creator-video-benchmark`. | None locally. | Added the package command and a path-safe bundle containing MP4, contact sheet, benchmark JSON, product manifest, scene-cache report, and quality report. | Complete benchmark plus JSON/path-safety and FFmpeg validation. | Latest report is schema-versioned, publishable, covers all asset kinds, both text families, both pace plans, pronunciation, avatar fixture, CTA, collision QA, temporal QA, and scoped cache reuse. | Subjective avatar and brand review only. |
| Human visual-readiness remediation | **Complete — local.** Structural and encoded visual readiness are separate, and the readable benchmark passes both. | None locally. | Added exact CTA sampling, production/debug policy, arrow/click/typing presentation, seeded SaaS workflow, readable landscape product assets, presenter-safe compositing, populated treatments, and transition-boundary sampling. | Positive and deliberately broken visual-readiness fixtures; interaction tests; final evidence sheets and reports. | Exact CTA appears in all three samples; four movements, three clicks, and one typing sequence are receipted; known labels render at 24 px minimum; presenter luma threshold is 32; all ten treatments and 57 boundary samples pass. | Subjective final video, avatar, and brand approval only. |
