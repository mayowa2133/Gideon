# Creator-video visual-readiness matrix

Updated: 2026-07-21

This is the live evidence ledger for the human-reviewed creator-video remediation goal. A row becomes **Complete — local** only when implementation, positive and negative tests, encoded benchmark evidence, and documentation agree. Structural validity alone is not visual-readiness evidence.

| Requirement | Baseline finding | Planned/implemented change | Required evidence | Status | External confirmation |
| --- | --- | --- | --- | --- | --- |
| Truthful readiness states | `publishable` previously meant only that no automated structural gate failed. | Added separate `structurallyPublishable` and `humanReviewReady`; compatibility `publishable` requires both, while final export still requires approval. | Contract tests, stored reports, renderer UI, benchmark report. | **Complete — local.** Both benchmark outcomes pass; omitted CTA proves structural true/human false. | Final subjective approval only. |
| Visible CTA | The old gate checked a reserved CTA scene, not encoded pixels. | Rendered exact copy and stored interval, rectangle, type, contrast, and encoded samples. | Positive receipt and deliberately omitted-CTA negative fixture. | **Complete — local.** Scene 020 shows the exact copy for 4,500 ms; 46 pt Arial, 15.4:1 contrast, and 3/3 informative samples. | Final CTA copy/brand approval only. |
| Production/debug policy | Blue guide-like outlines and test styling remained visible. | Added explicit policy, production default, metadata-only diagnostics, and forbidden-pattern rejection. | Production/debug and visible-timecode fixtures; final receipt. | **Complete — local.** Canonical mode is production and forbidden-pattern list is empty. | None. |
| Arrow pointer and motion | Interaction cue was a pulsing blue circle/box. | Added repository-native arrow geometry, `(1,1)` tip hotspot, eased continuous motion, pre-click dwell, and separate click feedback. | Interpolation/hotspot/timing tests and motion strip. | **Complete — local.** Receipt records 4 moves, 1 long, 3 short, and 3 clicks. | Product-specific allowed actions only. |
| Progressive typing | No readable incremental typing was demonstrated. | Added deterministic 45–70 ms character timing, word pauses, caret, completion dwell, cancellation, and secret redaction. | State/timing/redaction/cancellation tests and typing strip. | **Complete — local.** “Maya Chen” progresses visibly and secrets-redacted receipt passes. | None. |
| Realistic local workflow | Benchmark source was `testsrc2` plus moving rectangles. | Replaced it with a deterministic seeded local SaaS contact-edit workflow. | Source manifest, product assets, interaction strip, and manual inspection. | **Complete — local.** Contacts → search Maya Chen → open record → set Qualified → save → Changes saved. | Real-product access is optional, not needed for this fixture. |
| Product readability | Earlier scale gate checked blueprint focus only. | Propagated readable regions, landscape source framing, known-label checks, zoom, and post-action dwell. | Per-scene receipt and deliberately unreadable fixtures. | **Complete — local.** Five workflow labels pass at a 24 px minimum. | Accessibility threshold sign-off only. |
| Presenter exposure | Human review found excessively dark presenter scenes. | Moved dimming behind the keyed presenter and added encoded luma checks. | Bright/dark/layout fixtures and exposure receipt. | **Complete — local.** Seven presenter scenes pass minimum average luma 32; the conceptual treatment intentionally omits the presenter to preserve readable content. | Real-avatar realism remains external. |
| Informative treatments | Conceptual and other treatments could contain excessive blank space. | Required distinct populated content for all ten kinds and retained temporal media. | Positive and deliberately empty fixtures; overview/key sheets. | **Complete — local.** All ten treatment kinds are populated. | Brand styling approval only. |
| Transition clipping | Branding/disclosure clipped during transitions. | Added rectangle/signal validation at boundary−100 ms, boundary, and boundary+100 ms. | 57 timestamp receipt and negative clipping fixture. | **Complete — local.** No failing scenes or clipped elements. | None. |
| Upgraded benchmark | Old benchmark rendered `energetic` from test patterns and lacked visual evidence. | Made readable canonical, seeded the workflow, and emitted all required path-bounded artifacts. | Complete benchmark bundle under ignored `tmp/`. | **Complete — local.** All declared artifacts exist and are non-empty. | None. |
| Review UI | UI previously exposed a single automated readiness flag. | Shows structural/human states and detailed CTA, interaction, production, readability, exposure, and transition receipts. | Typecheck, desktop build, deterministic UI inspection. | **Complete — local.** Older receipt-less renders require regeneration; approval is disabled until both states pass. | UX acceptance only. |
| Final manual audit | Previous automated pass contradicted human inspection. | Inspect overview, key frames, motion, typing, CTA, treatments, presenter, and every boundary. | Written answers to the ten audit questions and final MP4 path. | **Complete — local.** The final v8 MP4, overview/key sheets, motion and typing strips, CTA samples, all treatment classes, presenter scenes, and all 57 boundary samples were inspected. | Final subjective release approval remains human. |

## Baseline and current evidence

- Reviewed artifact: `tmp/creator-video-benchmark/renders/benchmark-script/creator-video-energetic.mp4`.
- `evaluateCreatorVideoQuality` sets `publishable` when no gate has status `fail`; subjective avatar review does not block it.
- The CTA gate currently proves only that the last blueprint scene reserves the configured duration.
- The benchmark currently chooses the `energetic` plan and creates its source from FFmpeg `testsrc2` plus animated boxes.
- The compositor currently draws cursor cues as a pulsing circle plus rectangular outline.
- Existing generated media remains ignored and must not be committed.
- Current candidate: `tmp/creator-video-benchmark/renders/benchmark-script/creator-video-readable.mp4`.
- Current reports record 1080×1920 H.264/AAC, 47,399 ms duration, −14 LUFS, 553 ms maximum silence, a 0.2414 repeated-frame ratio with no unexpected freeze longer than 800 ms, temporal result `pass`, `structurallyPublishable: true`, and `humanReviewReady: true`.
- The manual v8 audit confirmed progressive typing, an arrow pointer with separate click feedback, readable terminal/feature/conceptual evidence, unobscured presenter layouts, a dark caption backing across light transitions, the exact CTA throughout its sampled interval, and no clipping across 57 boundary samples.
