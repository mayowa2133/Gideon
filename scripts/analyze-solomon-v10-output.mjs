import fs from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";
import { measureV10Motion } from "./lib/creator-story-v10-motion.mjs";

const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),".."),output=path.join(root,"tmp","solomon-creator-story-v10-performance"),master=process.env.SOLOMON_V10_MASTER??path.join(output,"final","solomon-creator-story-v10-performance-master-NOT-FOR-PUBLICATION.mp4"),baseline=JSON.parse(await fs.readFile(path.join(output,"baseline","v9-authoritative-baseline.json"),"utf8")),quality=JSON.parse(await fs.readFile(path.join(output,"reports","media-quality-report.json"),"utf8"));
const current=await measureV10Motion(master,[{id:"opening",fromSeconds:0,toSeconds:4.4},{id:"mechanism",fromSeconds:14,toSeconds:24.5},{id:"payoff",fromSeconds:27.5,toSeconds:32},{id:"cta",fromSeconds:32,toSeconds:36}]),v9=baseline.v9.motion,referenceAverage=average(baseline.referenceMeasurements.map(({motion})=>motion.averageFrameChange));
const report={schemaVersion:"10.1",methodVersion:"common-decoded-luma-v10.1",v9:{path:baseline.v9.path,sha256:baseline.v9.sha256,motion:v9},v10:{path:master,sha256:quality.masterSha256,motion:current},references:baseline.referenceMeasurements.map(({path,sha256,motion})=>({path,sha256,motion})),improvements:{hookComprehension:"Cause-and-effect is shown with Applied, a cursor action, and Interviewing in the opening proof chain.",contactRevealTimingSeconds:1.8,productOccupancy:"Large isolated evidence objects replace desktop-page framing.",requiredTextReadability:quality.ocr.requiredCoverage,semanticMotion:"Every manifest scene has a semantic event; decoded movement is reported separately so decorative drift cannot satisfy the contract alone.",transitionTypes:quality.transitions.boundaries,mascotFaces:quality.mascot.renderedFaceStateCount,mascotGestures:quality.mascot.renderedGestureStateCount,payoff:"One combined state replaces V9’s four-screen carousel.",cta:"Mascot looks, holds, taps, confirms, and returns gaze around one truthful save action.",audio:quality.decoded.loudness,privacy:{bannedPassed:quality.ocr.banned.passed,privateMatches:quality.ocr.privateMatches,disclosurePassed:quality.ocr.singleDisclosurePassed}},targets:{medianAtLeast:4.5,continuousAtLeast:4.5,lowMotionAtMostSeconds:2},referenceAverageFrameChange:referenceAverage,automatedParityClaimed:false,humanReferenceQualityRequired:true};
await fs.writeFile(path.join(output,"reports","v9-v10-reference-comparison.json"),`${JSON.stringify(report,null,2)}\n`);await fs.writeFile(path.join(output,"reports","v9-v10-reference-comparison.md"),`# V9 → V10 → reference comparison\n\n| Metric | V9 | V10 | Target / context |\n|---|---:|---:|---:|\n| Median movement | ${v9.medianFrameChange.toFixed(3)} | ${current.medianFrameChange.toFixed(3)} | ≥ 4.5 |\n| Continuous movement | ${v9.continuousMovementExcludingCuts.toFixed(3)} | ${current.continuousMovementExcludingCuts.toFixed(3)} | ≥ 4.5 |\n| Near-static frames | ${v9.nearStaticFramePercent.toFixed(2)}% | ${current.nearStaticFramePercent.toFixed(2)}% | lower is better |\n| Longest low-motion interval | ${v9.longestLowMotionSeconds.toFixed(2)} s | ${current.longestLowMotionSeconds.toFixed(2)} s | ≤ 2.0 s |\n| Average frame change | ${v9.averageFrameChange.toFixed(3)} | ${current.averageFrameChange.toFixed(3)} | references avg ${referenceAverage.toFixed(3)} |\n\nV10 improves proof size, cause-and-effect, one-state payoff, presenter acting, and CTA performance. Automated metrics do not establish reference parity; the human checklist remains blocked.\n`);process.stdout.write(`${JSON.stringify(report,null,2)}\n`);
function average(values){return values.length?values.reduce((sum,value)=>sum+value,0)/values.length:0;}
