import type {V8AudioFrame} from "../../shared/solomonRobotV8";
export function analyzeRobotNarrationPcm(samples:Float32Array,sampleRate:number,fps=30):V8AudioFrame[]{const width=Math.max(1,Math.round(sampleRate/fps));const rows:V8AudioFrame[]=[];let previous=0;for(let start=0,frame=0;start<samples.length;start+=width,frame+=1){let sum=0;for(let index=start;index<Math.min(samples.length,start+width);index+=1)sum+=samples[index]!**2;const rms=Math.min(1,Math.sqrt(sum/width)*4);const onset=Math.max(0,Math.min(1,(rms-previous)*5));rows.push({frame,rms,speaking:rms>.055,onset,phraseBoundary:previous>.08&&rms<.04,emphasized:onset>.55,pitchDelta:0});previous=rms;}return rows;}
